namespace REST_API.Models.Enums
{
    public enum StatusCode
    {
        OK,
        INVALID_REQUEST,
        DATABASE_ERROR,
        INVALID_EMAIL,
        INVALID_PASSWORD,
        EMAIL_ALREADY_EXISTS
    }
}